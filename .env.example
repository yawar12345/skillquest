# Frontend has no build-time secrets anymore — the admin passcode is
# configured server-side in api/local.settings.json (see
# api/local.settings.json.example) and, in production, as Static Web App
# application settings.
